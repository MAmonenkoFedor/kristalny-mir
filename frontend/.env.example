# URL бэкенда NestJS.
# ВАЖНО: это значение «вшивается» в сборку Next.js на этапе build,
# поэтому задавай его ДО `npm run build` на сервере.
# Прод: NEXT_PUBLIC_API_URL=https://api.crystalwt.by
NEXT_PUBLIC_API_URL=http://localhost:4000
